# configs/spark.yaml

app_name: CryptoQuant

master: local[*]

spark:
  app_name: btc-sentiment-platform
  master: local[*]

streaming:
  silver_trigger_seconds: 10
  gold_trigger_seconds: 10

gold:
  window_seconds: 300
  watermark_seconds: 400
  cron_minutes: 5
  sentiment_endpoint: http://host.docker.internal:8000/predict
  sentiment_timeout_seconds: 10
  sentiment_max_concurrent_requests: 4
  sentiment_chunk_size: 50

delta:
  bronze: delta/bronze
  silver: delta/silver
  gold: delta/gold

checkpoints:
  bronze: delta/checkpoint/bronze
  silver: delta/checkpoint/silver
  gold: delta/checkpoint/gold

kafka:
  bootstrap_servers: crypto-kafka:29092
  subscribe: btc_reddit,btc_yt,btc_news,btc_telegram
  starting_offsets: latest
  fail_on_data_loss: false

spark_configs:
  spark.sql.extensions: io.delta.sql.DeltaSparkSessionExtension
  spark.sql.catalog.spark_catalog: org.apache.spark.sql.delta.catalog.DeltaCatalog

  # 🔥 ADD THESE (CRITICAL)
  spark.jars.packages: io.delta:delta-spark_2.12:3.2.0
  spark.jars.ivy: /tmp/.ivy2

  spark.sql.shuffle.partitions: 64
  spark.default.parallelism: 64
  spark.sql.adaptive.enabled: true
  spark.sql.adaptive.coalescePartitions.enabled: true
  spark.sql.warehouse.dir: /tmp/spark-warehouse
  spark.sql.execution.arrow.pyspark.enabled: true
  spark.ui.enabled: true
